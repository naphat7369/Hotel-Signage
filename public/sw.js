const SHELL='stayscreen-shell-v1';
self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(SHELL);const r=await fetch('/');const html=await r.clone().text();await cache.put('/',r);const urls=[...html.matchAll(/(?:src|href)="(\/assets\/[^" ]+)"/g)].map(m=>m[1]);await cache.addAll([...new Set(urls)]);await self.skipWaiting()})()));
self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));
self.addEventListener('fetch',event=>{const u=new URL(event.request.url);if(u.origin!==location.origin||u.pathname.startsWith('/api/')||event.request.method!=='GET')return;if(event.request.mode==='navigate'&&u.pathname==='/player'){event.respondWith(fetch(event.request).catch(()=>caches.match('/')));return}if(u.pathname.startsWith('/assets/'))event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request).then(async r=>{if(r.ok){const c=await caches.open(SHELL);await c.put(event.request,r.clone())}return r})));});
